org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Disable Kotlin incremental compilation to avoid incremental cache root mismatch errors
kotlin.incremental=false
## Stabilize Gradle/Kotlin on Windows (avoid file watcher and cache issues)
# (Remove experimental/classpath snapshot overrides that triggered warnings)
